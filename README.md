I try to update the build in the 'dist' directory accordingly, so to use, just serve the index.HTML file in the 'dist' folder.
Easiest way is to use Bun.

```
bun dist/index.html
```
or
```
bun index.html
```
with terminal at /dist
